<ul>
  {{#each filteredList as |category|}}
      <li{{action "selectCategory" category}}>{{category.name}}</li>
    {{category-list selectedCategory='setCategory' list=category.children root=false}}
  {{/each}}
</ul>
